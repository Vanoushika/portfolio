{
  "name": "portfolio",
  "private": false,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "tw:init": "tailwindcss init -p"
  },
  "dependencies": { ... },
  "devDependencies": { ... }
}
